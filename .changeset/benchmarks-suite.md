---
---

Add a cross-library benchmark suite (`packages/generate-validators/bench`)
comparing generated mjst validators against Ajv and Zod, and populate the
Benchmarks section of the root and `generate-validators` READMEs with the
measured throughput and prepare-a-validator numbers.
