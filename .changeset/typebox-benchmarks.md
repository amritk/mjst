---
---

Add TypeBox (`TypeCompiler`) to the cross-library validator benchmark suite
alongside Ajv and Zod, and add its throughput and compile-cost numbers to the
Benchmarks tables in the root and `generate-validators` READMEs. TypeBox is
registered with uuid/email format checkers so every library validates the same
contract.
