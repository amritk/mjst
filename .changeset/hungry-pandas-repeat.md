---
---

Add `docs/api-caching-plan.md` — the strategy for contract-declared edge
caching in `@amritk/api`, covering prior art in cache invalidation, the
Cloudflare Workers Caching platform constraints, and the freshness tiers.
Documentation only; no published package changes.
