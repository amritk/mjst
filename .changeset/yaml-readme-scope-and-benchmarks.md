---
---

Refresh the `@amritk/yaml` README: re-measure the benchmark tables, correct the
conformance count to 397/402 with the fifth expected failure explained, document
the scope details the differential fuzzing settled (flow collections as keys,
property-only sequence entries, core-tag coercion, duplicate collection keys),
and add a table of the documents where this parser, `yaml`, and `js-yaml`
genuinely disagree.
