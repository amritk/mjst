---
---

docs: add a Benchmarks section to the `@amritk/generate-parsers` and `@amritk/resolve-refs` READMEs, documenting what each package's `bench/` suite measures and representative `bun run bench` numbers.
