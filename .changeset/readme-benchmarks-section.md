---
---

docs: add a Benchmarks section to the `@amritk/generate-parsers`, `@amritk/resolve-refs`, and `@amritk/lint` READMEs, documenting what each package's `bench/` suite measures and representative `bun run bench` numbers. Adds a new `bench/` harness (and `bench` script) to `@amritk/lint` that lints real-world OpenAPI specs with the recommended ruleset.
