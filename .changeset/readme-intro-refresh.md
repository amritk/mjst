---
---

docs: refresh the root README — move the Packages table up front, expand the "What is mjst?" overview to cover the full toolbox (test-data generation, linting, adapters, `$ref` resolution, runtime validation, YAML parsing), and update the header tagline.
