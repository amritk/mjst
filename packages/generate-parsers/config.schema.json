{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "buildSchema options",
  "description": "Options for the buildSchema function in @amritk/generate-parsers.",
  "type": "object",
  "properties": {
    "typesOnly": {
      "type": "boolean",
      "x-icon": "🏷️",
      "default": false,
      "description": "Generate only TypeScript type definitions without parser functions. Runtime helper files (validators, isObject) are also omitted since they are only needed for parsers."
    },
    "logWarnings": {
      "type": "boolean",
      "x-icon": "⚠️",
      "default": false,
      "description": "Emit a console.warn in the generated parsers for every input key that is not declared in the schema's properties. Useful for detecting schema drift or unexpected data shapes at runtime."
    },
    "strict": {
      "type": "boolean",
      "x-icon": "🚫",
      "default": false,
      "description": "Generate parsers that throw on type/shape mismatches (wrong type, missing required property, enum/pattern/min/max violations) instead of coercing invalid input to default values. When a schema sets additionalProperties: false, undeclared keys throw too; otherwise they are still allowed."
    },
    "stripUnknown": {
      "type": "boolean",
      "x-icon": "🧹",
      "default": false,
      "description": "Build each parser's result from the schema's declared properties only, silently dropping undeclared input keys at every nesting level (zod's .strip()). Extras are never a validation error, so this composes with strict (which still throws on wrong types and missing required properties) and yields to additionalProperties: false, which rejects rather than strips in strict mode."
    }
  }
}
