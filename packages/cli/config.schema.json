{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mjst CLI config",
  "description": "Generate TypeScript parsers and type definitions from JSON Schemas.\n\nYou can supply options via CLI flags or a JSON config file. CLI flags always take precedence over config file values.",
  "$comment": "Supply options either via CLI flags or a JSON config file — both approaches are shown below.",
  "type": "object",
  "allOf": [
    { "oneOf": [{ "required": ["schema"] }, { "required": ["schemaDir"] }] },
    { "oneOf": [{ "required": ["outDir"] }, { "required": ["outFile"] }] }
  ],
  "properties": {
    "schema": {
      "type": "string",
      "x-cli-flag": "--schema <path>",
      "x-icon": "📄",
      "description": "Path to the schema to process. With the default 'json' input this is a JSON Schema file that is read and parsed. With any other input format it is a JS/TS module that exports a schema, which is loaded and converted to JSON Schema via the matching adapter. Either 'schema' or 'schemaDir' is required.",
      "examples": ["./schema.json"]
    },
    "schemaDir": {
      "type": "string",
      "x-cli-flag": "--schema-dir <dir>",
      "x-icon": "🗂️",
      "description": "Path to a directory of JSON Schema files. When set, the CLI walks the directory recursively, generates parsers for every '*.json' schema it finds, and mirrors the directory layout under outDir (each schema lands in its own subdirectory). The runtime helpers are emitted once into a shared outDir/_helpers/ that every nested parser imports from. Mutually exclusive with 'schema'; when both are present 'schemaDir' wins. Only JSON Schema input is supported in this mode.",
      "examples": ["./schemas"]
    },
    "input": {
      "type": "string",
      "enum": ["json", "typebox", "zod", "valibot", "effect"],
      "default": "json",
      "x-cli-flag": "--input <format>",
      "x-icon": "🔌",
      "description": "Source format of the schema. 'json' (default) reads a JSON Schema file directly. Any other format loads 'schema' as a module and converts it to JSON Schema with the matching adapter. Supported: 'typebox', 'zod' (zod v4+), 'valibot' (with @valibot/to-json-schema), and 'effect' — each requires the corresponding library installed in your project."
    },
    "export": {
      "type": "string",
      "x-cli-flag": "--export <name>",
      "x-icon": "📦",
      "description": "Which export of the schema module to use when 'input' is not 'json'. Defaults to the default export, or the sole named export when the module has exactly one."
    },
    "outDir": {
      "type": "string",
      "x-cli-flag": "--out-dir <dir>",
      "x-icon": "📁",
      "description": "Output directory for generated TypeScript files. The directory is created automatically if it does not exist. Subdirectories are created as needed when a generated file includes a nested path. Mutually exclusive with 'outFile'.",
      "examples": ["./generated"]
    },
    "outFile": {
      "type": "string",
      "x-cli-flag": "--out-file <file>",
      "x-icon": "📄",
      "description": "Output everything to a single TypeScript file instead of a directory. Every generated definition is concatenated into one self-contained file and the cross-file imports are dropped. Mutually exclusive with 'outDir'. Currently supported only together with 'typesOnly'.",
      "examples": ["./generated/schema.ts"]
    },
    "typesOnly": {
      "type": "boolean",
      "x-cli-flag": "--types-only",
      "x-icon": "🏷️",
      "default": false,
      "description": "Generate only TypeScript type definitions without parser functions. Useful when you only need the type shapes and do not need runtime validation."
    },
    "build": {
      "type": "boolean",
      "x-cli-flag": "--build",
      "x-icon": "🔨",
      "default": false,
      "description": "Compile the generated TypeScript files to .js and .d.ts output. A temporary tsconfig is written to the output directory, tsc is invoked, and the intermediate .ts source files are removed when compilation succeeds."
    },
    "logWarnings": {
      "type": "boolean",
      "x-cli-flag": "--log-warnings",
      "x-icon": "⚠️",
      "default": false,
      "description": "Emit a console.warn in the generated parsers for every input key that is not declared in the schema's properties. Useful for detecting schema drift or unexpected data shapes at runtime."
    },
    "strict": {
      "type": "boolean",
      "x-cli-flag": "--strict",
      "x-icon": "🚫",
      "default": false,
      "description": "Generate parsers that throw on type/shape mismatches (wrong type, missing required property, enum/pattern/min/max violations) instead of coercing invalid input to default values. When a schema sets additionalProperties: false, undeclared keys throw too; otherwise they are still allowed."
    },
    "stripUnknown": {
      "type": "boolean",
      "x-cli-flag": "--strip-unknown",
      "x-icon": "🧹",
      "default": false,
      "description": "Build each parser's result from the schema's declared properties only, silently dropping undeclared input keys at every nesting level (zod's .strip()). Extras are never a validation error, so this composes with strict (which still throws on wrong types and missing required properties) and yields to additionalProperties: false, which rejects rather than strips in strict mode."
    },
    "readonly": {
      "type": "boolean",
      "x-cli-flag": "--readonly",
      "x-icon": "🔒",
      "default": false,
      "description": "Emit every property, array, and record in the generated type definitions as readonly, producing deeply immutable types. Affects type definitions only; the generated parsers still build and return plain objects."
    },
    "helpers": {
      "type": "string",
      "enum": ["package", "embedded"],
      "x-cli-flag": "--helpers <mode>",
      "x-icon": "🧰",
      "description": "Controls how generated parsers reference their runtime helpers. 'package' emits imports from @amritk/helpers (requires it to be installed in the consumer project). 'embedded' ships the helper source under outDir/_helpers/ so the output is self-contained. When omitted, the CLI auto-detects: it picks 'package' only when @amritk/helpers is a declared dependency (dependencies/devDependencies) of the nearest package.json above outDir, otherwise 'embedded'. Requiring a declaration (rather than mere resolvability) keeps auto-detected 'package' output portable across pnpm/isolated installs, where an undeclared transitive @amritk/helpers is not reachable at runtime."
    },
    "typeSuffix": {
      "type": "string",
      "x-cli-flag": "--type-suffix <suffix>",
      "x-icon": "🏷️",
      "default": "",
      "description": "Suffix appended to every generated type name derived from a $ref (e.g. 'Object' turns Contact into ContactObject). Defaults to no suffix. The root type name is used verbatim and is unaffected."
    },
    "importExt": {
      "type": "string",
      "enum": ["js", "ts"],
      "default": "ts",
      "x-cli-flag": "--import-ext <ext>",
      "x-icon": "🔗",
      "description": "Extension emitted on every relative import specifier in the generated output (cross-file $ref imports, the index barrel, and embedded-helper imports). 'ts' (default) emits the literal on-disk paths so the generated .ts sources load directly under Bun, Node's type stripping (Node 22.6+ with --experimental-strip-types, unflagged from 22.18/23), and tsc when the consumer sets allowImportingTsExtensions — no build step required. 'js' is the standard TS NodeNext form ('./x.js' resolving to a sibling x.ts) for output you compile; it is selected automatically under 'build' since tsc refuses to emit from .ts specifiers. Passing --import-ext ts together with --build is an error."
    },
    "rootType": {
      "type": "string",
      "x-cli-flag": "--root-type <name>",
      "x-icon": "🌱",
      "description": "Name for the root type of a single --schema run (e.g. 'Program' yields parseProgram / validateProgramShape). When omitted, the name is derived from the schema's title, falling back to the schema filename in PascalCase (spec-plan.json → SpecPlan) and then to 'Document'. Not supported with --schema-dir, where each schema derives its own root type from its filename.",
      "examples": ["Program"]
    },
    "config": {
      "type": "string",
      "x-cli-flag": "--config <path>",
      "x-icon": "⚙️",
      "description": "Path to a JSON config file. Keys match the option names in this schema (schema, schemaDir, outDir, outFile, typesOnly, build, logWarnings, strict, readonly, helpers, typeSuffix, importExt, rootType). CLI flags take precedence over config file values.",
      "examples": ["./mjst.config.json"]
    }
  },
  "examples": [
    {
      "schema": "./schema.json",
      "outDir": "./generated"
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "typesOnly": true
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "build": true
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "logWarnings": true
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "strict": true
    }
  ],
  "x-example-titles": [
    "Minimal — generate parsers and types",
    "Types only — skip parser functions",
    "Build — emit .js and .d.ts",
    "Log warnings — warn on unknown input properties",
    "Strict — throw on invalid input"
  ]
}
