{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mjst config",
  "description": "Generate TypeScript parsers and type definitions from JSON Schemas, with optional README generation.\n\nYou can supply options via CLI flags or a JSON config file. CLI flags always take precedence over config file values.",
  "$comment": "Supply options either via CLI flags or a JSON config file — both approaches are shown below.",
  "type": "object",
  "required": ["schema", "outDir"],
  "$defs": {
    "Requirement": {
      "title": "Requirement",
      "description": "A runtime or toolchain prerequisite listed under the Requirements section.",
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name of the required tool or runtime.",
          "examples": ["Bun", "Node.js", "Git"]
        },
        "version": {
          "type": "string",
          "description": "Minimum version constraint shown alongside the name.",
          "examples": [">=1.1.0", ">=18", ">=2.39"]
        },
        "link": {
          "type": "string",
          "format": "uri",
          "description": "URL to the tool's download or documentation page.",
          "examples": ["https://bun.sh", "https://nodejs.org"]
        }
      }
    },
    "Badge": {
      "title": "Badge",
      "description": "A shields.io badge rendered in the README header.",
      "type": "object",
      "required": ["label", "message", "color"],
      "properties": {
        "label": {
          "type": "string",
          "description": "Left-hand label text of the badge.",
          "examples": ["version", "license", "coverage"]
        },
        "message": {
          "type": "string",
          "description": "Right-hand message text of the badge.",
          "examples": ["v1.0.0", "MIT", "100%"]
        },
        "color": {
          "type": "string",
          "description": "Hex color (without #) or named color for the message half of the badge.",
          "examples": ["6366f1", "22c55e", "blue"]
        },
        "logo": {
          "type": "string",
          "description": "Simple Icons slug for an icon shown on the left of the badge.",
          "examples": ["npm", "typescript", "github"]
        },
        "logoColor": {
          "type": "string",
          "description": "Color of the logo. Only applied when logo is set.",
          "examples": ["white", "black"]
        },
        "link": {
          "type": "string",
          "format": "uri",
          "description": "URL the badge links to when clicked.",
          "examples": ["https://npmjs.com/package/my-pkg"]
        }
      }
    },
    "InstallStep": {
      "title": "InstallStep",
      "description": "A single installation step rendered as an optional heading followed by a code block.",
      "type": "object",
      "required": ["command"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Optional heading shown above the code block.",
          "examples": ["Install dependencies", "Install globally"]
        },
        "command": {
          "type": "string",
          "description": "Shell command displayed inside the code block.",
          "examples": ["bun install", "npm install -g mjst"]
        },
        "language": {
          "type": "string",
          "description": "Syntax-highlighting language identifier for the fenced code block.",
          "default": "zsh",
          "examples": ["zsh", "bash", "sh", "powershell"]
        }
      }
    },
    "UsageExample": {
      "title": "UsageExample",
      "description": "A named usage example rendered as a heading, optional description, and code block.",
      "type": "object",
      "required": ["title", "command"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Heading for this example block.",
          "examples": ["Generate parsers and types", "Types only — skip parser functions"]
        },
        "command": {
          "type": "string",
          "description": "Command or code snippet displayed inside the code block.",
          "examples": ["mjst --schema ./schema.json --outDir ./generated"]
        },
        "language": {
          "type": "string",
          "description": "Syntax-highlighting language identifier for the fenced code block.",
          "default": "bash",
          "examples": ["bash", "zsh", "sh"]
        },
        "description": {
          "type": "string",
          "description": "Explanatory text rendered below the code block.",
          "examples": ["Generates both runtime parsers and TypeScript type definitions."]
        }
      }
    }
  },
  "properties": {
    "schema": {
      "type": "string",
      "x-cli-flag": "--schema <path>",
      "x-icon": "📄",
      "description": "Path to the JSON Schema file to process. The schema is read, parsed, and used to generate TypeScript source files.",
      "examples": ["./schema.json"]
    },
    "outDir": {
      "type": "string",
      "x-cli-flag": "--outDir <dir>",
      "x-icon": "📁",
      "description": "Output directory for generated TypeScript files. The directory is created automatically if it does not exist. Subdirectories are created as needed when a generated file includes a nested path.",
      "examples": ["./generated"]
    },
    "typesOnly": {
      "type": "boolean",
      "x-cli-flag": "--types-only",
      "x-icon": "🏷️",
      "default": false,
      "description": "Generate only TypeScript type definitions without parser functions. Useful when you only need the type shapes and do not need runtime validation."
    },
    "config": {
      "type": "string",
      "x-cli-flag": "--config <path>",
      "x-icon": "⚙️",
      "description": "Path to a JSON config file. Keys match the option names in this schema (schema, outDir, typesOnly). CLI flags take precedence over config file values.",
      "examples": ["./mjst.config.json"]
    },
    "installation": {
      "type": "object",
      "x-icon": "📦",
      "description": "Controls the Installation section of the generated README.\n\nProvide one or more ordered steps — each renders as an optional heading and a fenced code block. Omit this property entirely to fall back to the auto-detected package manager install command.",
      "properties": {
        "steps": {
          "type": "array",
          "description": "Ordered installation steps. Each step becomes a code block under the Installation heading.",
          "items": { "$ref": "#/$defs/InstallStep" },
          "minItems": 1
        },
        "notes": {
          "type": "string",
          "description": "Optional markdown text rendered after all installation steps. Use this for prerequisites, platform notes, or post-install instructions.",
          "examples": ["Requires Bun v1.1 or later.", "See [docs](https://example.com) for advanced setup."]
        }
      }
    },
    "badges": {
      "type": "array",
      "x-icon": "🎖️",
      "description": "Extra shields.io badges appended after the auto-generated version, license, and TypeScript badges in the README header.",
      "items": { "$ref": "#/$defs/Badge" },
      "examples": [
        [{ "label": "coverage", "message": "100%", "color": "22c55e" }]
      ]
    },
    "usage": {
      "type": "object",
      "x-icon": "🚀",
      "description": "Controls the Usage section of the generated README.\n\nProvide named examples — each renders as a heading, optional description, and fenced code block. Omit this property to auto-derive an example from the required schema properties that carry x-cli-flag annotations.",
      "properties": {
        "examples": {
          "type": "array",
          "description": "Ordered usage examples shown under the Usage heading.",
          "items": { "$ref": "#/$defs/UsageExample" },
          "minItems": 1
        },
        "notes": {
          "type": "string",
          "description": "Optional markdown text rendered after all usage examples.",
          "examples": ["Run `mjst --help` to see all available flags."]
        }
      }
    },
    "features": {
      "type": "array",
      "x-icon": "✨",
      "description": "Bullet-pointed feature highlights rendered in a Features section between the Overview and Installation sections.\n\nKeep each item to one sentence — readers scan this list to decide whether the tool suits their needs.",
      "items": {
        "type": "string",
        "description": "A single feature statement.",
        "examples": [
          "Generates fully type-safe parsers from any JSON Schema 2020-12 document",
          "Zero runtime dependencies — emits plain TypeScript"
        ]
      },
      "minItems": 1
    },
    "requirements": {
      "type": "array",
      "x-icon": "📋",
      "description": "Runtime or toolchain prerequisites listed in a Requirements section above Installation.\n\nEach entry renders as a linked name and optional version constraint so readers know what to install before they begin.",
      "items": { "$ref": "#/$defs/Requirement" },
      "minItems": 1
    },
    "contributing": {
      "type": "object",
      "x-icon": "🤝",
      "description": "Controls the Contributing section at the bottom of the generated README.\n\nProvide either a file path to link to or inline markdown text, or both.",
      "properties": {
        "file": {
          "type": "string",
          "description": "Relative path to a CONTRIBUTING file. Rendered as a link: 'See CONTRIBUTING.md for guidelines.'",
          "default": "./CONTRIBUTING.md",
          "examples": ["./CONTRIBUTING.md", "./docs/contributing.md"]
        },
        "text": {
          "type": "string",
          "description": "Inline markdown text rendered below the file link. Use this for a short summary so readers get the gist without clicking through.",
          "examples": ["PRs are welcome. Please open an issue first to discuss what you would like to change."]
        }
      }
    },
    "changelog": {
      "type": "object",
      "x-icon": "📝",
      "description": "Controls the Changelog section at the bottom of the generated README.",
      "properties": {
        "file": {
          "type": "string",
          "description": "Relative path to a CHANGELOG file. Rendered as a link beneath the section heading.",
          "default": "./CHANGELOG.md",
          "examples": ["./CHANGELOG.md", "./RELEASES.md"]
        },
        "text": {
          "type": "string",
          "description": "Inline markdown text shown in the Changelog section. Use this when you link to GitHub Releases instead of a local file.",
          "examples": ["See [GitHub Releases](https://github.com/org/repo/releases) for the full history."]
        }
      }
    },
    "license": {
      "type": "object",
      "x-icon": "⚖️",
      "description": "Controls the License section at the bottom of the generated README. The license identifier is taken from package.json; this property lets you customise the file path and add a notice.",
      "properties": {
        "file": {
          "type": "string",
          "description": "Relative path to the LICENSE file. Rendered as a link alongside the SPDX identifier from package.json.",
          "default": "./LICENSE",
          "examples": ["./LICENSE", "./LICENSE.md"]
        },
        "notice": {
          "type": "string",
          "description": "Short additional notice rendered below the license link, e.g. for dual-licensing or attribution requirements.",
          "examples": ["Copyright © 2025 Amrit Kochar. All rights reserved."]
        }
      }
    }
  },
  "examples": [
    {
      "schema": "./schema.json",
      "outDir": "./generated"
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "typesOnly": true
    },
    {
      "schema": "./schema.json",
      "outDir": "./generated",
      "features": [
        "Generates fully type-safe parsers from any JSON Schema 2020-12 document",
        "Supports $ref, $dynamicRef, and recursive schema definitions",
        "Types-only mode for projects that do not need runtime validation",
        "Zero runtime dependencies — emits plain TypeScript"
      ],
      "requirements": [
        { "name": "Bun", "version": ">=1.1.0", "link": "https://bun.sh" }
      ],
      "installation": {
        "steps": [
          { "command": "bun add mjst" }
        ],
        "notes": "Requires Bun v1.1 or later."
      },
      "usage": {
        "examples": [
          {
            "title": "Generate parsers and types",
            "command": "mjst --schema ./schema.json --outDir ./generated"
          },
          {
            "title": "Types only — skip parser functions",
            "command": "mjst --schema ./schema.json --outDir ./generated --types-only",
            "description": "Emits only TypeScript type definitions without runtime validation code."
          }
        ],
        "notes": "Run `mjst --help` to see all available flags."
      },
      "contributing": {
        "file": "./CONTRIBUTING.md",
        "text": "PRs are welcome. Please open an issue first to discuss what you would like to change."
      },
      "changelog": {
        "text": "See [GitHub Releases](https://github.com/amritk/mjst/releases) for the full history."
      },
      "license": {
        "file": "./LICENSE",
        "notice": "Copyright © 2025 Amrit Kochar."
      }
    }
  ],
  "x-example-titles": [
    "Minimal — generate parsers and types",
    "Types only — skip parser functions",
    "Full — with installation and usage documentation"
  ]
}
