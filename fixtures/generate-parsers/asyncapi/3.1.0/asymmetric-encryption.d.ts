export type AsymmetricEncryptionObject = {
    /** A short description for security scheme. */
    description?: string;
    /** The type of the security scheme. */
    type: "asymmetricEncryption";
};
