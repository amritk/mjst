export type BindingsKafka030OperationObject = {
    /** Id of the consumer group. */
    groupId?: unknown;
    /** Id of the consumer inside a consumer group. */
    clientId?: unknown;
    /** The version of this binding. If omitted, 'latest' MUST be assumed. */
    bindingVersion?: "0.3.0";
};
